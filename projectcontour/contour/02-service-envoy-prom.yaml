---
apiVersion: v1
kind: Service
metadata:
  name: envoy-monitor
  namespace: projectcontour
  labels:
    app: envoy
spec:
  selector:
    app: envoy
  type: ClusterIP
  clusterIP: None
  ports:
  - name: prometheus-metrics
    port: 8002
    targetPort: 8002
    protocol: TCP
