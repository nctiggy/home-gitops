apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: prometheus-helm-values
  namespace: monitoring
spec:
  keys:
    - values.yaml
  path: secrets/monitoring/prometheus/prometheus-helm-values
  type: Opaque
