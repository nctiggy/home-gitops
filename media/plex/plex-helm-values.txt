claimToken: "$PLEX_CLAIM_TOKEN"
advertiseIp: "plex.$DOMAIN_ROOT"
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: contour
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/tls-acme: "true"
  hosts: [plex.$DOMAIN_ROOT]
  tls:
  - secretName: plex
    hosts:
    - plex.$DOMAIN_ROOT
