apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config
  namespace: media
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 56Gi
  storageClassName: vxflexos
  volumeMode: Filesystem
  volumeName: k8s-66bd22879d
